# Ignore all extensionless files
# (ignores all, then adds dirs and files with extensions)
*
!/**/
!*.*

# Un-ignore makefiles
!Makefile

# Ignored build artefacts
*.o
*.so*
*.a
*S.a
*.pinfo
*.dep
*.gcno
